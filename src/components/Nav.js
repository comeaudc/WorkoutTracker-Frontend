const Nav = () => {
    return (
        <nav>
            <button>New Workout</button>
            <button>Prev Workout</button>
            <button>Stats</button>
        </nav>
    )
}

export default Nav;